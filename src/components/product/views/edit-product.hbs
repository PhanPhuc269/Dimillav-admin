<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2c3e50; /* Dark background */
            color: #ecf0f1; /* Light text */
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 50px auto;
            padding: 20px;
            background-color: #34495e; /* Form background */
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }
        h1 {
            text-align: center;
            color: #1abc9c; /* Primary color */
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
            color: #ecf0f1;
        }
        input[type="text"],
        input[type="number"],
        textarea,
        select,
        input[type="file"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #7f8c8d;
            border-radius: 4px;
            background-color: #2c3e50;
            color: #ecf0f1;
        }
        textarea {
            resize: vertical;
        }
        .form-group.multi-input {
            display: flex;
            gap: 10px;
        }
        button {
            width: 100%;
            padding: 10px;
            background-color: #1abc9c;
            border: none;
            border-radius: 4px;
            color: #ecf0f1;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #16a085;
        }
        .alert.alert-danger {
            color: #e74c3c;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Update Product</h1>
        <form action="/product/update/{{product.slug}}" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="{{#if formData.name}}{{formData.name}}{{else}}{{product.name}}{{/if}}" />
                {{#if errors.name}}
                    <div class="alert alert-danger">{{errors.name}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <textarea id="description" name="description">{{#if formData.description}}{{formData.description}}{{else}}{{product.description}}{{/if}}</textarea>
                {{#if errors.description}}
                    <div class="alert alert-danger">{{errors.description}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="originalPrice">Original Price:</label>
                <input type="number" id="originalPrice" name="originalPrice" value="{{#if formData.originalPrice}}{{formData.originalPrice}}{{else}}{{product.originalPrice}}{{/if}}" required />
                {{#if errors.originalPrice}}
                    <div class="alert alert-danger">{{errors.originalPrice}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="salePrice">Sale Price:</label>
                <input type="number" id="salePrice" name="salePrice" value="{{#if formData.salePrice}}{{formData.salePrice}}{{else}}{{product.salePrice}}{{/if}}" />
                {{#if errors.salePrice}}
                    <div class="alert alert-danger">{{errors.salePrice}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="saleDuration">Sale Duration (hours):</label>
                <input type="number" id="saleDuration" name="saleDuration" value="{{#if formData.saleDuration}}{{formData.saleDuration}}{{else}}{{product.saleDuration}}{{/if}}" />
                {{#if errors.saleDuration}}
                    <div class="alert alert-danger">{{errors.saleDuration}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="material">Material:</label>
                <input type="text" id="material" name="material" value="{{#if formData.material}}{{formData.material}}{{else}}{{product.material}}{{/if}}" />
                {{#if errors.material}}
                    <div class="alert alert-danger">{{errors.material}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="style">Style:</label>
                <input type="text" id="style" name="style" value="{{#if formData.style}}{{formData.style}}{{else}}{{product.style}}{{/if}}" />
                {{#if errors.style}}
                    <div class="alert alert-danger">{{errors.style}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="gender">Gender:</label>
                <select id="gender" name="gender" required>
                    <option value="Men" {{#if (eq product.gender 'Men')}}selected{{/if}}>Men</option>
                    <option value="Women" {{#if (eq product.gender 'Women')}}selected{{/if}}>Women</option>
                    <option value="Unisex" {{#if (eq product.gender 'Unisex')}}selected{{/if}}>Unisex</option>
                </select>
                {{#if errors.gender}}
                    <div class="alert alert-danger">{{errors.gender}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="warranty">Warranty:</label>
                <input type="text" id="warranty" name="warranty" value="{{#if formData.warranty}}{{formData.warranty}}{{else}}{{product.warranty}}{{/if}}" />
                {{#if errors.warranty}}
                    <div class="alert alert-danger">{{errors.warranty}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="brand">Brand:</label>
                <input type="text" id="brand" name="brand" value="{{#if formData.brand}}{{formData.brand}}{{else}}{{product.brand}}{{/if}}" />
                {{#if errors.brand}}
                    <div class="alert alert-danger">{{errors.brand}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="type">Type:</label>
                <input type="text" id="type" name="type" value="{{#if formData.type}}{{formData.type}}{{else}}{{product.type}}{{/if}}" />
                {{#if errors.type}}
                    <div class="alert alert-danger">{{errors.type}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="category">Category:</label>
                <input type="text" id="category" name="category" value="{{#if formData.category}}{{formData.category}}{{else}}{{product.category}}{{/if}}" />
                {{#if errors.category}}
                    <div class="alert alert-danger">{{errors.category}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="tags">Tags (comma separated):</label>
                <input type="text" id="tags" name="tags" value="{{#if formData.tags}}{{formData.tags}}{{else}}{{#each product.tags}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}{{/if}}" />
                {{#if errors.tags}}
                    <div class="alert alert-danger">{{errors.tags}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="availability">Availability:</label>
                <select id="availability" name="availability" required>
                    <option value="In Stock" {{#if (eq product.availability 'In Stock')}}selected{{/if}}>In Stock</option>
                    <option value="Out of Stock" {{#if (eq product.availability 'Out of Stock')}}selected{{/if}}>Out of Stock</option>
                </select>
                {{#if errors.availability}}
                    <div class="alert alert-danger">{{errors.availability}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="stock">Stock:</label>
                <div class="multi-input">
                    <input type="number" name="stock[0][size]" placeholder="Size" value="{{#if formData.stock.[0].size}}{{formData.stock.[0].size}}{{else}}{{product.stock.[0].size}}{{/if}}" required />
                    <input type="text" name="stock[0][color]" placeholder="Color" value="{{#if formData.stock.[0].color}}{{formData.stock.[0].color}}{{else}}{{product.stock.[0].color}}{{/if}}" required />
                    <input type="number" name="stock[0][quantity]" placeholder="Quantity" value="{{#if formData.stock.[0].quantity}}{{formData.stock.[0].quantity}}{{else}}{{product.stock.[0].quantity}}{{/if}}" required />
                </div>
                {{#if errors.stock.[0]}}
                    <div class="alert alert-danger">{{errors.stock.[0]}}</div>
                {{/if}}
            </div>
            <div class="form-group">
                <label for="images">Image(s):</label>
                <input type="file" id="images" name="images" multiple />
                {{#if errors.images}}
                    <div class="alert alert-danger">{{errors.images}}</div>
                {{/if}}
            </div>
            <button type="submit">Update Product</button>
        </form>
    </div>
</body>
</html>